export enum PostPrivacy {
  USER = 'USER',
  JAM = 'JAM',
  ALL = 'ALL',
}

export enum RoleType {
  ADMIN = 'ADMIN',
  MEMBER = 'MEMBER',
  OTHER = 'OTHER'
}

export enum PaletteMode {
  LIGHT = 'light',
  DARK = 'dark',
}