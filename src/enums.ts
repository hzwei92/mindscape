export enum PostPrivacy {
  USER = 'USER',
  JAM = 'JAM',
  ALL = 'ALL',
}

export enum RoleType {
  NONE = 'NONE',
  ADMIN = 'ADMIN',
  MEMBER = 'MEMBER',
  SUBSCRIBER = 'SUBSCRIBER',
  OTHER = 'OTHER',
};

export enum PaletteMode {
  LIGHT = 'light',
  DARK = 'dark',
}